language: julia

os:
  - linux
  - osx

arch:
  - x64
  - amd64
  # - ppc64le
  - arm64

julia:
  - 1.3
  - nightly

matrix:
  allow_failures:
    - julia: nightly

addons:
  apt_packages:
    - gfortran

branches:
  only:
    - master
    - /^v\d+\.\d+(\.\d+)?(-\S*)?$/ # tags

before_install:
  - if [ `uname` == "Linux" ]; then unset DY_LIBRARY_PATH; fi
  - if [ `uname` == "Darwin" ]; then brew update; brew cask uninstall oclint; brew install gcc; fi
  - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi

jobs:
  include:
    - stage: Documentation
      julia: 1.3
      os: linux
      script:
        - julia --project=docs -e 'using Pkg; Pkg.instantiate(); Pkg.add(PackageSpec(path=pwd()))'
        - julia --project=docs docs/make.jl
      after_success: skip

after_success:
  - julia -e 'if Sys.islinux() && string(VERSION)[1:3] == "1.3"
      using Pkg; Pkg.add("Coverage"); using Coverage; Coveralls.submit(Coveralls.process_folder()); Codecov.submit(process_folder())
    end'
